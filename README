# start signaling server
* npm run signaling

# start node webrtc client
* npm run client

# serve browser client code (requires global http-server install)
* http-server

# open localhost:8082 in browser
run in console
* var myConnection = peer.connect('node-client');
* myConnection.send('some data');

# optional - build client bundle with (not working)
npm run build
